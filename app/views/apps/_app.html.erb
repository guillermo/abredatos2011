<li>
  <%= link_to image_tag((app.screenshot.try(:small) || ''), :with => '77', :height => 64, :title => app.short_name), app %>
  <p class="name"><%= link_to app.short_name, app %></p>
  <p class="category"><%= app.category %></p>
</li>
