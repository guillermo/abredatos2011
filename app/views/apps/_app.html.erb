<li>
  <%= link_to image_tag((app.screenshot.try(:small) || ''), :width => 77, :height => 64, :title => app.short_name), app %>
  <p class="name"><%= link_to truncate(app.short_name, :length => 18), app %></p>
  <p class="category"><%= app.category %></p>
</li>
